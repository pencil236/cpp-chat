# C++ 全栈聊天项目学习日志

> 教程：[恋恋风辰的 C++ 全栈项目实战](https://gitbookcpp.llfc.club/sections/cpp/project/day01.html)
>
> 学习方式：按照教程逐节亲手实现，以作者源码作为对照；必要的兼容性修改需要单独记录。

## 当前状态

- 当前阶段：00 — 环境检查（已完成）
- 当前目标：进入 Day 01，创建正式的 `llfcchat` Qt Widgets 工程
- 开始日期：2026-09-09
- 项目状态：Qt 开发环境已验证，准备开始 Day 01

## 我的基础

- 能使用 C++ 编写算法题
- 工程 C++、Qt、网络编程和分布式开发经验待补充
- 采用“项目驱动 + 按需补基础”的学习方式

## 开发环境

> 完成环境检查后填写具体版本，不凭印象记录。

- 操作系统：Windows 10 Pro，64 位（内部版本 26200）
- IDE：VS Code 1.136.2；Qt Creator 9.0.0（`E:\Qt\Tools\QtCreator`）
- Qt：5.15.2 MinGW 8.1 64-bit（`E:\Qt\5.15.2\mingw81_64`）
- C++ 编译器：MinGW-w64 GCC 8.1.0（`E:\Qt\Tools\mingw810_64`）；服务端工具链到对应阶段再决定
- 构建工具：qmake 3.1（Qt 5.15.2）；后续依赖构建按需安装 CMake / Ninja
- Git：2.53.0.windows.1，可用
- Boost：尚未配置
- gRPC / Protobuf：尚未配置
- Node.js：尚未配置
- Redis：尚未配置
- MySQL：尚未配置

## 学习原则

1. 项目名称、目录、类名、函数名、界面和通信流程尽量与教程保持一致。
2. 不直接复制完整成品；先亲手实现，再用作者源码核对。
3. 每次只完成一个可运行、可验收的学习单元。
4. 依赖使用到时再安装，不提前部署完整分布式系统。
5. 教程代码因版本问题无法编译时，采用最小兼容性修改并记录原因。
6. 每个阶段完成后编译、运行、验收并提交 Git。

## 对话命名规范

统一使用：

```text
聊天项目｜阶段编号｜主题｜目标
```

当前及后续对话示例：

- `聊天项目｜00｜环境检查｜运行第一个 Qt 窗口`
- `聊天项目｜01｜登录界面｜完成 LoginDialog`
- `聊天项目｜02｜注册界面｜用信号槽切换页面`
- `聊天项目｜03｜HTTP 管理类｜理解单例和异步请求`
- `聊天项目｜04｜GateServer｜处理 HTTP POST 和 JSON`
- `聊天项目｜05｜验证码服务｜连接 Node.js 与 gRPC`
- `聊天项目｜06｜注册功能｜打通客户端到数据库`
- `聊天项目｜07｜TCP 客户端｜连接聊天服务器`
- `聊天项目｜08｜Asio 服务器｜实现会话与消息收发`

## 当前任务清单

### 阶段 00：环境检查

- [x] 确认 IDE 及版本
- [x] 确认 Qt 及版本
- [x] 确认 C++ 编译器及版本
- [x] 确认构建工具
- [x] 确认 Git 可用
- [x] 创建最小 Qt Widgets 工程
- [x] 成功编译并显示第一个窗口
- [x] 记录环境差异和解决方法
- [x] 创建阶段提交

### 阶段 01：Day 01 登录与注册界面

- [ ] 创建 `llfcchat` Qt Widgets 工程
- [ ] 设置主窗口尺寸、标题和图标
- [ ] 添加 Qt 资源文件
- [ ] 创建 `LoginDialog`
- [ ] 创建 `RegisterDialog`
- [ ] 使用信号与槽切换登录和注册界面
- [ ] 添加并加载 QSS 样式
- [ ] 对照教程截图验收界面
- [ ] 记录本节涉及的 C++ / Qt 知识
- [ ] 创建阶段提交

## 进度记录

### 2026-09-09

- 创建学习日志。
- 确定先检查开发环境，再开始 Day 01。
- 本机环境检查：Git 2.53.0 可用；未发现 Qt、Qt Creator、MSVC、MinGW、CMake 或 Ninja。
- 核对作者源码：客户端为 qmake 的 `.pro` 工程，且使用 Qt 5 的 `QRegExp`；服务端 Visual Studio 工程指定 v142 工具集。
- 最初计划使用 Qt 5.15.2 MSVC 2019 64-bit；考虑到本机已有 VS Code 且暂不安装 Visual Studio，客户端改用 Qt 5.15.2 MinGW 8.1 64-bit + qmake。
- VS Code 仅作为编辑器，不提供 C++ 编译器；客户端由 Qt 配套 MinGW 编译。服务端原版 `.vcxproj` 所需的 MSVC/v142 到对应阶段再决定是否安装 Build Tools，或改用 CMake/MinGW。
- 已安装 Qt 5.15.2、MinGW-w64 GCC 8.1.0 和 Qt Creator 9.0.0，合计约 1.24 GB，均位于 `E:\Qt`。
- 已将 Qt、MinGW 和 Qt Creator 的 `bin` 目录加入当前用户 PATH，并设置 `QTDIR=E:\Qt\5.15.2\mingw81_64`；重启终端或 VS Code 后生效。
- 创建 `00-qt-smoke-test`，使用 qmake + mingw32-make 成功生成并启动 64 位 Qt Widgets 程序，窗口标题为 `Qt environment check`。
- 尝试安装 VS Code 的 Microsoft C/C++ 扩展两次，均因扩展市场连接超时失败；不影响 Qt Creator、qmake 和命令行构建，后续网络恢复后再补装。

## 问题与知识点

### 当前需要补充

- Qt 对象树和父子对象生命周期
- 信号与槽
- 头文件、源文件、编译与链接
- 构造函数、析构函数和资源管理

### 当前阻塞

- 无。VS Code C/C++ 扩展尚未安装是可选增强项，不阻塞 Qt 开发。

## 教程兼容性修改记录

| 日期 | 教程原写法 | 实际修改 | 修改原因 | 是否影响后续 |
| --- | --- | --- | --- | --- |
| 2026-09-09 | 服务端使用 Visual Studio v142；客户端可使用 MSVC Qt Kit | 客户端先使用 Qt 5.15.2 MinGW 8.1 64-bit，VS Code 作为编辑器 | 避免当前安装完整 Visual Studio | 客户端无实质影响；服务端章节需选择 MSVC Build Tools 或迁移 CMake/MinGW |

## 每阶段完成标准

只有同时满足以下条件，阶段才能标记为完成：

- 代码可以从干净状态成功构建。
- 程序可以运行，功能符合本阶段目标。
- 能说明关键类、函数和数据流的作用。
- 已对照教程或作者源码检查差异。
- 已在本日志记录问题、解决方案和下一步。
- 已创建对应的 Git 提交。

## 下一步

初始化 Git 仓库并创建阶段 00 提交，然后进入 Day 01，创建正式的 `llfcchat` Qt Widgets 工程。
