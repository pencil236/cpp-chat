# 跨对话协作说明

## 1. 文件用途

本文件是 `cpp-chat` 项目所有对话共享的协作入口。任何新对话开始处理本项目之前，都应先阅读本文件，再按任务查阅其他资料。

### 项目文档地图

| 文件 | 用途 | 何时阅读 | 何时更新 |
| --- | --- | --- | --- |
| `AGENTS.md` | 长期有效的跨对话职责、协作边界、防冲突规则和交接格式 | 每个对话开始处理项目前 | 只有协作方式或长期规则发生变化时 |
| `LEARNING_LOG.md` | 总体学习路线、个人基础、开发环境、阶段清单、兼容性记录和阶段完成标准 | 判断项目路线、学习背景或阶段目标时 | 阶段状态、环境、总体计划或长期阻塞变化时 |
| `project_notes.md` | 各阶段的详细实现记录、源码讲解、知识边界、验收流程和自测问题 | 处理当前阶段的代码、讲解、排错或验收时 | 完成一个有长期参考价值的学习单元后 |

三份文档的边界如下：

- `AGENTS.md` 不保存容易过期的具体阶段待办。
- `LEARNING_LOG.md` 记录“整个项目准备怎样学、目前大致走到哪里”。
- `project_notes.md` 记录“某一阶段具体做了什么、为什么这样做、如何验收”。
- 临时任务以用户在对应对话中的最新要求为准，不必全部写进文档。
- 当前源码、`git status`、最近提交和实际构建结果是实现状态的最终依据；文档与它们冲突时，应先核实并修正文档。

当前 `LEARNING_LOG.md` 和 `project_notes.md` 被 `.gitignore` 忽略。它们可供同一工作区中的对话共享，但不会自动进入 Git 提交或出现在新的克隆中。未经用户要求，不要擅自改变这一跟踪策略。

## 2. 用户的总体目标

用户正在按照“恋恋风辰的 C++ 全栈项目实战”学习并亲手完成聊天项目，本地项目名为 `pencil-chat`。

核心目标不是尽快复制完整成品，而是：

1. 按教程阶段逐步实现，每次只推进一个可运行、可验收的学习单元。
2. 理解实际写到的 C++、Qt、网络和分布式知识，不提前堆入大量尚未学习的代码。
3. 每个阶段都经过构建、运行、人工验收、笔记整理和 Git 提交。
4. 教程与本机环境不兼容时，只做必要的最小修改，并记录原因。

## 3. 用户负责什么

除非用户在某次对话中明确要求代为修改，否则默认由用户负责：

- 在 Qt Creator 中亲手编写或调整教程代码。
- 运行程序，观察真实界面并完成需要视觉判断或点击操作的验收。
- 回答自测问题，确认自己理解了当前阶段的关键概念。
- 决定何时进入下一阶段，以及是否创建 Git 提交。
- 明确告诉当前对话本次要处理的具体任务。

对话不能把“给出答案”误当成用户已经掌握，也不能把“构建成功”误当成视觉和交互验收已经完成。

## 4. 当前执行对话负责什么

正在接受用户任务的对话是该任务的唯一负责人，应当：

1. 先检查本文件、相关笔记、源码、`git status` 和最近提交，再判断当前状态。
2. 在讲具体操作、展示代码或修改文件之前，先用清楚的语言完成“本次教学导入”：
   - **本次实现什么**：说明要增加或验证的用户可见功能，以及本次任务的边界。
   - **与前面有什么关系**：指出它承接了哪些已经完成的界面、类、信号、数据流或阶段成果，而不是只说“接着上一节”。
   - **为什么要实现**：说明该功能解决什么问题、在完整聊天项目中处于什么位置，以及为什么适合在当前阶段学习。
   - **怎样算完成**：给出可观察、可验证的完成标准。
3. 教学导入完成后，再说明本次涉及的文件、实现顺序和所需知识点；不要一上来就让用户照着步骤或代码操作。
4. 只处理用户在本次对话中明确提出的任务，不擅自推进后续阶段。
5. 默认以讲解、检查、定位问题和给出小步骤为主；只有用户明确要求修改、实现或修复时才直接编辑源码。
6. 修改前先确认工作区已有变化，保留用户和其他对话留下的未提交内容。
7. 修改后进行与风险相称的构建或检查，并明确区分：
   - 静态检查是否通过；
   - 是否成功构建；
   - 是否实际运行；
   - 是否经过人工视觉和交互验收。
8. 任务结束时更新“当前状态与交接记录”；若产生了新的长期知识，再更新 `project_notes.md`。

## 5. 其他对话负责什么

其他对话只有在用户明确分配任务后才开始工作。它们应当：

- 领取一个边界清楚、可以独立完成的任务，例如解释一个知识点、检查一组文件、排查一个构建错误或对照教程核验一小节。
- 不重复当前执行对话正在进行的工作。
- 不主动修改当前执行对话正在编辑的同一组文件。
- 不因为看到“下一步”就自动开始下一阶段。
- 完成后留下简短交接：检查或修改了什么、结论是什么、验证到了哪一步、还有什么需要用户或主对话继续做。
- 如果发现范围重叠、工作区存在来源不明的改动或笔记与源码矛盾，先报告冲突，不覆盖现有内容。

未经用户明确授权，其他对话不得替用户完成整章教程、批量重构项目或创建 Git 提交。

## 6. 多对话防冲突规则

1. 同一时间，同一个源码文件只交给一个对话修改。
2. 每个对话开始前运行 `git status --short`，结束前再次检查。
3. 不使用会丢弃现有修改的命令，例如 `git reset --hard` 或强制检出覆盖。
4. 构建产物位于构建目录，不把自动生成文件当成源码修改。
5. 源码、Git 状态、构建结果和人工运行结果的可信度高于旧笔记。
6. 需要跨阶段调整时，先向用户说明为什么超出原任务以及会影响哪些文件。

## 7. 如何确定当前状态与分工

项目阶段会持续变化，因此不要在本文件中长期写死某个阶段的具体待办。

每个对话都按下面的顺序确定当前状态：

1. 以用户在该对话中的最新要求确定本次任务。
2. 用 `git status`、最近提交和当前源码确认实际实现状态。
3. 阅读 `project_notes.md` 获取最近阶段的详细说明和遗留事项。
4. 阅读 `LEARNING_LOG.md` 了解总体路线；若它与源码不一致，以源码和较新的记录为准。

当前执行对话可以协助讲解、检查、排错、实现、构建或整理笔记，但具体做哪一项必须由用户本次要求决定。其他对话没有固定的“次要任务”，也不得根据旧记录自行推断；它们只执行用户明确分配给各自的任务。

如果用户确实安排了多个对话并行工作，应把临时分工写进下面的交接记录。任务完成后，将该记录标记为完成或删除，避免过期任务继续影响后续对话。

## 8. 交接记录格式

每次需要把工作交给另一个对话时，在本节顶部追加一条记录：

```text
日期：YYYY-MM-DD
负责对话：对话名称或任务主题
用户目标：本次明确要求
已完成：实际完成的检查或修改
涉及文件：文件列表；没有修改则写“无”
验证：未验证 / 静态检查 / 构建通过 / 已运行 / 用户人工验收通过
遗留事项：下一位只需要继续处理的内容
```

当前并行任务：无。
